<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Products</title>
  </head>
  <body>
    <h1>Display products using EJS loops</h1>
    <hr />
    <h2>Our products</h2>
    <ul>
      <% products.forEach((product) => { %>
      <li><%= product.name %></li>
      - $ <%= product.price %> <% }) %>
    </ul>
  </body>
</html>
