<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>User Data</title>
  </head>
  <body>
    <h1>Parsing Data from server to client</h1>
    <hr />
    <!-- Accessing data frm the server -->
    <h2>Welcome, <%=userName %>!</h2>
    <p>Age: <%=age %></p>
    <p>Is this user premium? <%=isPremiumUser %></p>
    <% if (isPremiumUser) { %>
    <p>You are a premium member. Congrats!</p>
    <% } else { %>
    <p>It's time to upgrade to premium</p>
    <% } %>
  </body>
</html>
