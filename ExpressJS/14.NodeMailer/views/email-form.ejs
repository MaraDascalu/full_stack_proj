<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>NodeMailer App - Email Form</title>
    <link rel="stylesheet" href="styles.css" />
  </head>
  <body>
    <div class="container">
      <h1 class="title">NodeMailer App</h1>
      <p class="description">
        Send emails easily using NodeMailer. Just fill in the form and click
        "Send Email" to get started.
      </p>
      <form
        action="/send-email"
        method="POST"
        class="email-form"
        id="email-form"
      >
        <input
          type="email"
          name="email"
          id="email"
          placeholder="Please enter the email"
          required
        />

        <textarea
          name="message"
          id="message"
          rows="5"
          placeholder="Please enter the message here"
          required
        ></textarea>

        <button type="submit" id="submit-button">Send Email</button>
      </form>

      <% if (status && message) { %>
      <div class="status-message"><%=message %></div>
      <% }%>
    </div>
  </body>
</html>
